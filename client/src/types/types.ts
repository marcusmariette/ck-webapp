export type MenuItem = {
    id: string;
    name: string;
    description: string;
    price: number;
    imgSrc: string;
};

export type LoginDetails = {
    username: string;
    password: string;
};
