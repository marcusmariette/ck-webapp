export type MenuItem = {
    id: number;
    name: string;
    description: string;
    price: number;
    imgSrc: string;
};

export type LoginDetails = {
    username: string;
    password: string;
};
